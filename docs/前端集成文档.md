# 前端集成文档

## 一、概述

本文档描述如何在前端Vue.js项目中集成PayPal支付功能。

### 1.1 技术栈

- Vue.js 3.x
- Vue Router
- Pinia（状态管理）
- Axios（HTTP请求）
- PayPal JS SDK

---

## 二、PayPal SDK 集成

### 2.1 SDK加载工具

创建 `src/utils/paypal-sdk.js`：

```javascript
/**
 * PayPal SDK 动态加载工具
 */

let isLoading = false;
let isLoaded = false;
let paypalPromise = null;

/**
 * 动态加载PayPal SDK
 * @param {Object} options - 配置选项
 * @returns {Promise} PayPal SDK实例
 */
export function loadPayPalSDK(options = {}) {
  const {
    clientId = import.meta.env.VITE_PAYPAL_CLIENT_ID,
    currency = 'USD',
    intent = 'capture',
    locale = 'en_US'
  } = options;

  // 已加载则直接返回
  if (window.paypal && isLoaded) {
    return Promise.resolve(window.paypal);
  }

  // 正在加载则返回现有Promise
  if (isLoading && paypalPromise) {
    return paypalPromise;
  }

  isLoading = true;

  paypalPromise = new Promise((resolve, reject) => {
    const script = document.createElement('script');
    
    // 构建SDK URL参数
    const params = new URLSearchParams({
      'client-id': clientId,
      currency,
      intent,
      locale
    });

    script.src = `https://www.paypal.com/sdk/js?${params.toString()}`;
    script.async = true;

    script.onload = () => {
      isLoaded = true;
      isLoading = false;
      resolve(window.paypal);
    };

    script.onerror = () => {
      isLoading = false;
      reject(new Error('PayPal SDK加载失败'));
    };

    document.head.appendChild(script);
  });

  return paypalPromise;
}

/**
 * 检查SDK是否已加载
 */
export function isSDKLoaded() {
  return isLoaded && !!window.paypal;
}
```

### 2.2 环境变量配置

创建 `frontend/.env`：

```bash
# API基础URL
VITE_API_BASE_URL=http://localhost:3000

# PayPal Client ID（沙箱）
VITE_PAYPAL_CLIENT_ID=your_sandbox_client_id
```

创建 `frontend/.env.production`：

```bash
# 生产环境API
VITE_API_BASE_URL=https://yourdomain.com

# PayPal Client ID（生产）
VITE_PAYPAL_CLIENT_ID=your_production_client_id
```

---

## 三、支付组件

### 3.1 PayPal按钮组件

创建 `src/components/payment/PayPalButton.vue`：

```vue
<template>
  <div class="paypal-button-container">
    <!-- 加载状态 -->
    <div v-if="loading" class="loading-placeholder">
      <span>正在加载支付按钮...</span>
    </div>
    
    <!-- PayPal按钮容器 -->
    <div ref="buttonContainer" id="paypal-button-container"></div>
    
    <!-- 错误提示 -->
    <div v-if="error" class="error-message">
      {{ error }}
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue';
import { loadPayPalSDK } from '@/utils/paypal-sdk';
import { createPaymentOrder, capturePayment } from '@/api/payment';

const props = defineProps({
  orderId: {
    type: String,
    required: true
  },
  amount: {
    type: Number,
    required: true
  },
  currency: {
    type: String,
    default: 'USD'
  }
});

const emit = defineEmits([
  'create-start',
  'create-success',
  'create-error',
  'approve-start',
  'payment-success',
  'payment-error',
  'payment-cancel'
]);

const loading = ref(true);
const error = ref(null);
const buttonContainer = ref(null);
let paypalInstance = null;

onMounted(async () => {
  await initializePayPal();
});

onUnmounted(() => {
  // 清理按钮
  if (buttonContainer.value) {
    buttonContainer.value.innerHTML = '';
  }
});

async function initializePayPal() {
  try {
    loading.value = true;
    error.value = null;

    // 加载PayPal SDK
    paypalInstance = await loadPayPalSDK({
      currency: props.currency,
      intent: 'capture'
    });

    // 渲染按钮
    await paypalInstance.Buttons(getButtonConfig()).render(buttonContainer.value);

    loading.value = false;
  } catch (err) {
    console.error('PayPal初始化失败:', err);
    error.value = '支付按钮加载失败，请刷新页面重试';
    loading.value = false;
    emit('create-error', err);
  }
}

function getButtonConfig() {
  return {
    // 按钮样式
    style: {
      layout: 'vertical',
      color: 'blue',
      shape: 'rect',
      label: 'paypal',
      height: 45
    },

    // 创建订单
    createOrder: async () => {
      try {
        emit('create-start');
        
        const response = await createPaymentOrder({
          orderId: props.orderId,
          returnUrl: `${window.location.origin}/payment/success`,
          cancelUrl: `${window.location.origin}/payment/cancel`
        });

        if (response.success) {
          emit('create-success', response.data);
          return response.data.paypalOrderId;
        } else {
          throw new Error(response.message || '创建支付订单失败');
        }
      } catch (error) {
        emit('create-error', error);
        throw error;
      }
    },

    // 用户批准支付后
    onApprove: async (data) => {
      try {
        emit('approve-start', data);
        
        const response = await capturePayment({
          paypalOrderId: data.orderID
        });

        if (response.success) {
          emit('payment-success', response.data);
        } else {
          throw new Error(response.message || '支付处理失败');
        }
      } catch (error) {
        emit('payment-error', error);
      }
    },

    // 取消支付
    onCancel: (data) => {
      emit('payment-cancel', data);
    },

    // 错误处理
    onError: (err) => {
      console.error('PayPal错误:', err);
      error.value = '支付过程中发生错误，请重试';
      emit('payment-error', err);
    }
  };
}
</script>

<style scoped>
.paypal-button-container {
  min-height: 45px;
}

.loading-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 45px;
  background: #f5f5f5;
  border-radius: 4px;
  color: #666;
}

.error-message {
  color: #d32f2f;
  font-size: 14px;
  margin-top: 10px;
}
</style>
```

### 3.2 支付状态组件

创建 `src/components/payment/PaymentStatus.vue`：

```vue
<template>
  <div class="payment-status">
    <div :class="['status-icon', statusClass]">
      <span v-if="status === 'success'">✓</span>
      <span v-else-if="status === 'failed'">✗</span>
      <span v-else>?</span>
    </div>
    
    <h2 class="status-title">{{ statusTitle }}</h2>
    <p class="status-message">{{ message }}</p>
    
    <div v-if="orderInfo" class="order-info">
      <p>订单号: {{ orderInfo.orderNo }}</p>
      <p>金额: {{ orderInfo.amount }} {{ orderInfo.currency }}</p>
    </div>
    
    <div class="actions">
      <button v-if="status === 'success'" @click="viewOrder" class="btn-primary">
        查看订单
      </button>
      <button v-else-if="status === 'failed'" @click="retryPayment" class="btn-primary">
        重新支付
      </button>
      <button @click="goHome" class="btn-secondary">
        返回首页
      </button>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { useRouter } from 'vue-router';

const props = defineProps({
  status: {
    type: String,
    default: 'pending' // success, failed, cancelled
  },
  message: {
    type: String,
    default: ''
  },
  orderInfo: {
    type: Object,
    default: null
  }
});

const router = useRouter();

const statusClass = computed(() => {
  return {
    'success': 'status-success',
    'failed': 'status-failed',
    'cancelled': 'status-cancelled'
  }[props.status] || 'status-pending';
});

const statusTitle = computed(() => {
  return {
    'success': '支付成功',
    'failed': '支付失败',
    'cancelled': '支付已取消'
  }[props.status] || '处理中';
});

function viewOrder() {
  router.push(`/orders/${props.orderInfo?.orderId}`);
}

function retryPayment() {
  router.push('/checkout');
}

function goHome() {
  router.push('/');
}
</script>

<style scoped>
.payment-status {
  text-align: center;
  padding: 40px 20px;
}

.status-icon {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px;
  font-size: 40px;
}

.status-success {
  background: #e8f5e9;
  color: #4caf50;
}

.status-failed {
  background: #ffebee;
  color: #f44336;
}

.status-cancelled {
  background: #fff3e0;
  color: #ff9800;
}

.status-title {
  margin-bottom: 10px;
}

.status-message {
  color: #666;
  margin-bottom: 20px;
}

.order-info {
  background: #f5f5f5;
  padding: 15px;
  border-radius: 8px;
  margin-bottom: 20px;
}

.actions {
  display: flex;
  gap: 10px;
  justify-content: center;
}

.btn-primary, .btn-secondary {
  padding: 10px 30px;
  border-radius: 4px;
  cursor: pointer;
}

.btn-primary {
  background: #0070ba;
  color: white;
  border: none;
}

.btn-secondary {
  background: white;
  color: #333;
  border: 1px solid #ccc;
}
</style>
```

---

## 四、页面集成

### 4.1 结账页面

创建 `src/views/Checkout.vue`：

```vue
<template>
  <div class="checkout-page">
    <div class="checkout-container">
      <!-- 步骤指示器 -->
      <div class="steps">
        <div class="step" :class="{ active: step >= 1 }">1. 购物车</div>
        <div class="step" :class="{ active: step >= 2 }">2. 收货信息</div>
        <div class="step" :class="{ active: step >= 3 }">3. 支付</div>
      </div>

      <!-- 订单摘要 -->
      <div class="order-summary">
        <h2>订单摘要</h2>
        <div class="cart-items">
          <div v-for="item in cartItems" :key="item.id" class="cart-item">
            <div class="item-info">
              <span class="item-name">{{ item.name }}</span>
              <span class="item-quantity">x {{ item.quantity }}</span>
            </div>
            <span class="item-price">${{ (item.price * item.quantity).toFixed(2) }}</span>
          </div>
        </div>
        <div class="order-total">
          <span>总计:</span>
          <span class="total-amount">${{ totalAmount.toFixed(2) }} {{ currency }}</span>
        </div>
      </div>

      <!-- 收货信息 -->
      <div class="shipping-form">
        <h2>收货信息</h2>
        <form @submit.prevent="handleSubmit">
          <div class="form-group">
            <label>收件人姓名 *</label>
            <input v-model="shippingAddress.recipientName" required />
          </div>
          <div class="form-group">
            <label>手机号 *</label>
            <input v-model="shippingAddress.phone" required />
          </div>
          <div class="form-group">
            <label>详细地址 *</label>
            <textarea v-model="shippingAddress.address" required></textarea>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>城市 *</label>
              <input v-model="shippingAddress.city" required />
            </div>
            <div class="form-group">
              <label>邮编</label>
              <input v-model="shippingAddress.postalCode" />
            </div>
          </div>
        </form>
      </div>

      <!-- 支付方式 -->
      <div class="payment-section">
        <h2>支付方式</h2>
        
        <!-- 未创建订单时显示确认按钮 -->
        <button 
          v-if="!orderId" 
          class="btn-confirm"
          @click="createOrder"
          :disabled="isCreatingOrder"
        >
          {{ isCreatingOrder ? '处理中...' : '确认订单并支付' }}
        </button>
        
        <!-- 已创建订单时显示PayPal按钮 -->
        <div v-else class="paypal-section">
          <p class="order-id">订单号: {{ orderNo }}</p>
          <PayPalButton
            :orderId="orderId"
            :amount="totalAmount"
            :currency="currency"
            @create-start="onCreateStart"
            @create-success="onCreateSuccess"
            @approve-start="onApproveStart"
            @payment-success="onPaymentSuccess"
            @payment-error="onPaymentError"
            @payment-cancel="onPaymentCancel"
          />
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue';
import { useRouter } from 'vue-router';
import { useCartStore } from '@/stores/cart';
import { createOrder } from '@/api/order';
import PayPalButton from '@/components/payment/PayPalButton.vue';

const router = useRouter();
const cartStore = useCartStore();

const step = ref(2);
const orderId = ref(null);
const orderNo = ref('');
const isCreatingOrder = ref(false);
const currency = ref('USD');

const cartItems = computed(() => cartStore.items);
const totalAmount = computed(() => cartStore.totalAmount);

const shippingAddress = ref({
  recipientName: '',
  phone: '',
  address: '',
  city: '',
  postalCode: '',
  country: 'CN'
});

async function createOrder() {
  if (!validateForm()) {
    alert('请填写完整的收货信息');
    return;
  }

  try {
    isCreatingOrder.value = true;
    
    const response = await createOrder({
      items: cartItems.value.map(item => ({
        productId: item.id,
        productName: item.name,
        quantity: item.quantity,
        price: item.price
      })),
      shippingAddress: shippingAddress.value,
      currency: currency.value
    });

    if (response.success) {
      orderId.value = response.data.orderId;
      orderNo.value = response.data.orderNo;
      step.value = 3;
    }
  } catch (error) {
    alert('创建订单失败，请重试');
  } finally {
    isCreatingOrder.value = false;
  }
}

function validateForm() {
  return shippingAddress.value.recipientName && 
         shippingAddress.value.phone && 
         shippingAddress.value.address &&
         shippingAddress.value.city;
}

function onCreateStart() {
  console.log('正在创建支付订单...');
}

function onCreateSuccess(data) {
  console.log('PayPal订单创建成功:', data);
}

function onApproveStart(data) {
  console.log('正在处理支付...', data);
}

function onPaymentSuccess(data) {
  cartStore.clearCart();
  router.push({
    path: '/payment/result',
    query: {
      status: 'success',
      orderId: data.orderId,
      transactionId: data.transactionId
    }
  });
}

function onPaymentError(error) {
  router.push({
    path: '/payment/result',
    query: {
      status: 'failed',
      message: error.message
    }
  });
}

function onPaymentCancel(data) {
  router.push({
    path: '/payment/result',
    query: {
      status: 'cancelled'
    }
  });
}
</script>
```

### 4.2 支付结果页面

创建 `src/views/PaymentResult.vue`：

```vue
<template>
  <div class="payment-result-page">
    <PaymentStatus
      :status="paymentStatus"
      :message="message"
      :order-info="orderInfo"
    />
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import { useRoute } from 'vue-router';
import PaymentStatus from '@/components/payment/PaymentStatus.vue';
import { getPaymentStatus } from '@/api/payment';

const route = useRoute();

const paymentStatus = computed(() => route.query.status || 'pending');
const message = computed(() => route.query.message || '');

const orderInfo = ref(null);

onMounted(async () => {
  const orderId = route.query.orderId;
  if (orderId && paymentStatus.value === 'success') {
    try {
      const response = await getPaymentStatus(orderId);
      if (response.success) {
        orderInfo.value = {
          orderId: response.data.orderId,
          orderNo: response.data.orderId,
          amount: response.data.amount,
          currency: response.data.currency
        };
      }
    } catch (error) {
      console.error('获取支付状态失败:', error);
    }
  }
});
</script>
```

---

## 五、API 封装

### 5.1 支付API

创建 `src/api/payment.js`：

```javascript
import request from './request';

/**
 * 创建PayPal支付订单
 */
export function createPaymentOrder(data) {
  return request.post('/payments/create-order', data);
}

/**
 * 捕获支付
 */
export function capturePayment(data) {
  return request.post('/payments/capture-order', data);
}

/**
 * 获取支付状态
 */
export function getPaymentStatus(orderId) {
  return request.get(`/payments/status/${orderId}`);
}
```

### 5.2 订单API

创建 `src/api/order.js`：

```javascript
import request from './request';

/**
 * 创建订单
 */
export function createOrder(data) {
  return request.post('/orders', data);
}

/**
 * 获取订单详情
 */
export function getOrder(orderId) {
  return request.get(`/orders/${orderId}`);
}

/**
 * 获取订单列表
 */
export function getOrders(params) {
  return request.get('/orders', { params });
}
```

### 5.3 请求封装

创建 `src/api/request.js`：

```javascript
import axios from 'axios';

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL + '/api',
  timeout: 30000
});

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  error => {
    return Promise.reject(error);
  }
);

// 响应拦截器
request.interceptors.response.use(
  response => {
    return response.data;
  },
  error => {
    const message = error.response?.data?.message || '请求失败';
    console.error('API Error:', message);
    return Promise.reject(error);
  }
);

export default request;
```

---

## 六、路由配置

```javascript
// src/router/index.js
import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/Home.vue')
  },
  {
    path: '/checkout',
    name: 'Checkout',
    component: () => import('@/views/Checkout.vue')
  },
  {
    path: '/payment/result',
    name: 'PaymentResult',
    component: () => import('@/views/PaymentResult.vue')
  },
  {
    path: '/payment/success',
    redirect: '/payment/result?status=success'
  },
  {
    path: '/payment/cancel',
    redirect: '/payment/result?status=cancelled'
  }
];

const router = createRouter({
  history: createWebHistory(),
  routes
});

export default router;
```

---

## 七、状态管理

### 7.1 购物车Store

创建 `src/stores/cart.js`：

```javascript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const useCartStore = defineStore('cart', () => {
  const items = ref([]);

  const totalAmount = computed(() => {
    return items.value.reduce((sum, item) => {
      return sum + item.price * item.quantity;
    }, 0);
  });

  const itemCount = computed(() => {
    return items.value.reduce((sum, item) => sum + item.quantity, 0);
  });

  function addItem(product) {
    const existing = items.value.find(item => item.id === product.id);
    if (existing) {
      existing.quantity += product.quantity || 1;
    } else {
      items.value.push({ ...product, quantity: product.quantity || 1 });
    }
  }

  function removeItem(productId) {
    const index = items.value.findIndex(item => item.id === productId);
    if (index > -1) {
      items.value.splice(index, 1);
    }
  }

  function updateQuantity(productId, quantity) {
    const item = items.value.find(item => item.id === productId);
    if (item) {
      item.quantity = quantity;
    }
  }

  function clearCart() {
    items.value = [];
  }

  return {
    items,
    totalAmount,
    itemCount,
    addItem,
    removeItem,
    updateQuantity,
    clearCart
  };
});
```

---

## 八、注意事项

### 8.1 SDK加载

- PayPal SDK只需加载一次，多次调用会复用
- SDK加载失败时会显示错误提示
- 建议在支付页面才加载SDK，减少首页加载时间

### 8.2 错误处理

- 所有API调用都需要try-catch处理
- 用户取消支付是正常流程，不应显示错误
- 支付失败时应提供重试选项

### 8.3 移动端适配

- PayPal按钮已适配移动端
- 确保结账页面在移动端可正常使用
- 测试不同屏幕尺寸下的支付流程
